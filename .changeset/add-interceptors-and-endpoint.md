---
"@fortifyjs/http": minor
---

Add typed interceptors (auth token, idempotency key) and `createEndpoint()` for compile-time safe routes with path params, optional query schema, and typed responses.
